import Link from 'next/link';
import React from 'react';
// import {Button} from '../components/Button';

// const PostLink = React.forwardRef(({ onClick }, ref) => (
//     <Button onClick={onClick} ref={ref}>
//       Text{/* <Button onClick={props.resetPopupState}>To main page</Button> */}
//     </Button>
// ))
// export default () => (
//     <PostLink/>);
